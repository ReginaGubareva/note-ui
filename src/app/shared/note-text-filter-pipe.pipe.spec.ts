import { NoteTextFilterPipePipe } from './note-text-filter-pipe.pipe';

describe('NoteTextFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NoteTextFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
